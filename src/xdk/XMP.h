#pragma once
#include "xmp/xmp.h"

// this is where you'd include every last .h file that you need that comes from XMP
// that way, you don't have to worry about which XMP header to include for your
// particular use case, as you can just include this one
